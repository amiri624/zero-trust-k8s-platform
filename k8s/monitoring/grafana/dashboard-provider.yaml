apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-source
data:
  dashboards.yaml: |
    apiVersion: 1
    providers:
    - name: 'default'
      folder: 'default'
      type: file
      options:
        path: /var/lib/grafana/dashboards
